# Public analytics identifiers. Tracking loads only after cookie consent.
VITE_GA_MEASUREMENT_ID=
VITE_META_PIXEL_ID=

# Server-only. Use the same random value in Portbase and this Vercel project.
# Never prefix this value with VITE_ or expose it to browser code.
PORTBASE_PROXY_TOKEN=
PORTBASE_INTERNAL_ORIGIN=https://portbase.app
